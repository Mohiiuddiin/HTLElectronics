@model HTLElectronics.Models.AboutPageContent
@{
    ViewBag.Title = "Create About Page";
}
<style>
    label {
        color: azure;
        font-weight: bold;
    }
</style>
@using (Html.BeginForm())
{

    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    if (Model.About != "")
    {
        @Html.HiddenFor(model => model.Id)
    }
    @**@

    <div style="background-color:black;padding-left: ; border: 0pt whitesmoke groove; border-radius: 5pt; margin-bottom:3% ;">

        <button type="button" class="btn btn-lg btn-block" style="margin-top: 3%; margin-bottom: 3%;background-color:darkslategrey;color:azure;font-weight:bold">Create About Page</button>

        <div class="col-md-12">
            <h4 class="btn btn-info" style="padding-left:35px;padding-right:35px;font-weight:bolder;font-size:large">Create New Product</h4>

        </div>
        <div class="row" style="">

            <div class="col-md-12">
                <div class="form-group col-md-12">
                    @Html.LabelFor(model => model.About, htmlAttributes: new { @class = "control-label" })
                    @*@Html.EditorFor(model => model.Product.Description, new { htmlAttributes = new { @class = "form-control col-md-12" } })*@
                    <textarea cols="80" id="About" name="About" rows="10" data-sample-short>
                    @if (Model.About != "")
                        {
                            @Html.Raw(Model.About)
                        }
                    </textarea>
                    @Html.ValidationMessageFor(model => model.About, "", new { @class = "text-danger" })

                </div>
            </div>

            <div class="col-md-12">
                <div class="form-group col-md-4">
                    <input class="btn btn-info col-md-4" type="submit" value="Create" />
                </div>
            </div>
        </div>

    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
<script>
    // We need to turn off the automatic editor creation first.
    CKEDITOR.disableAutoInline = true;

    CKEDITOR.replace('About');
</script>